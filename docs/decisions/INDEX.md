# Anchored Decision Records — Index

Navigable index of rip-cage's ADRs. Each ADR captures a cross-cutting, load-bearing decision; firmness is **per-decision** (`FIRM` / `FLEXIBLE` / `EXPLORATORY` / `PROPOSED`), not per-document, so the doc-level **Status** column below is a coarse signal — open the ADR for the firmness of a specific decision (Dn).

**How to consult:** scan the *By topic* map for the scope you're touching, open the ADR, and check the relevant decision's firmness before contradicting it. Decisions evolve **in place** (per the in-place-evolution convention — the global methodology rule that ADRs reflect target architecture; note: rip-cage's local ADR-011 is shell-completions, not this rule; see bead `rip-cage-bwwm` for the cross-repo namespace decision) — git history carries predecessor wording; there are no supersession chains. ADRs link upstream via a `## canonical_refs` section where present.

When a decision evolves, edit the ADR in place and update this row. Drift between this index and `ls docs/decisions/ADR-*.md` is the failure this file exists to prevent.

## By topic

- **Safety / fail-loud:** [001](ADR-001-fail-loud-pattern.md), [004](ADR-004-phase1-hardening.md), [023](ADR-023-secret-path-mount-denylist.md), [024](ADR-024-prompt-injection-threat-model.md), [025](ADR-025-host-adoptable-dcg-policy.md)
- **Containers / cage model:** [002](ADR-002-rip-cage-containers.md), [016](ADR-016-cage-host-network-awareness.md), [026](ADR-026-containment-mediation-identity.md)
- **CLI / UX:** [003](ADR-003-agent-friendly-cli.md), [009](ADR-009-ux-overhaul.md), [011](ADR-011-shell-completions.md)
- **Testing / CI:** [013](ADR-013-test-coverage.md)
- **Egress / network:** [012](ADR-012-egress-firewall.md), [026](ADR-026-containment-mediation-identity.md)
- **Composition / mediators:** [026](ADR-026-containment-mediation-identity.md), [005](ADR-005-ecosystem-tools.md), [006](ADR-006-multi-agent-architecture.md), [027](ADR-027-agent-substrate-projection.md)
- **SSH / identity:** [014](ADR-014-push-less-cage.md), [017](ADR-017-ssh-agent-forwarding-default.md), [018](ADR-018-macos-ssh-agent-discovery.md), [020](ADR-020-ssh-identity-routing.md), [022](ADR-022-ssh-allowlist.md)
- **Auth / credentials:** [010](ADR-010-auth-refresh.md)
- **Config substrate:** [021](ADR-021-layered-rip-cage-config.md), [016](ADR-016-cage-host-network-awareness.md)
- **Beads / persistence:** [007](ADR-007-beads-dolt-container-resilience.md)
- **Toolchain provisioning:** [005](ADR-005-ecosystem-tools.md), [015](ADR-015-mise-toolchain-provisioning.md)
- **Release / open-source:** [008](ADR-008-open-source-publication.md)
- **Multi-agent:** [006](ADR-006-multi-agent-architecture.md)
- **pi-coding-agent:** [019](ADR-019-pi-coding-agent-support.md), [027](ADR-027-agent-substrate-projection.md)

## All ADRs

| ADR | Title | Scope | Status | Summary |
|---|---|---|---|---|
| [001](ADR-001-fail-loud-pattern.md) | Fail-Loud Error Handling | safety | Accepted | Silent fallbacks in the safety harness downgrade guarantees; fail loud with actionable remedies |
| [002](ADR-002-rip-cage-containers.md) | Rip Cage Containers | containers | Accepted (D2a provisional; D5 revised 2026-06-22) | Docker sandbox for Claude Code agents — OrbStack, persistent containers, bypassPermissions; D2a base = debian:trixie (glibc 2.41, legacy-iptables pin), provisional spike. **D5 (revised 2026-06-22, rip-cage-r9n4 / wlwc — SHIPPED): agent-writable hook-registration limitation CLOSED — the DCG command-guard hook now lives in baked-root-owned `/etc/claude-code/managed-settings.json` (CC's highest-precedence layer; hooks merge un-suppressibly, deny-wins; verified against shipped CC binary, 15/15 regression); `~/.claude/settings.json` stays agent-writable (no chmod; separate root-owned layer added); consciously reverses the equivalence-axis rejection for the guard under ADR-024 prompt-injection. Command-string guards (DCG, ssh-bypass) demoted from welded floor → composable default-on recipes above containment (DCG registration now ALSO tamper-resistant via the managed layer; ssh-bypass stays in the agent-writable `settings.json`, not moved); floor = containment** |
| [003](ADR-003-agent-friendly-cli.md) | Agent-Friendly CLI Interface | cli-ux | Accepted | Structured JSON output, dry-run, allowed-roots validation, agent context in CLAUDE.md |
| [004](ADR-004-phase1-hardening.md) | Phase 1 Hardening | safety | Accepted | Conditional Dolt server, resource limits, credential health check, expanded test suite |
| [005](ADR-005-ecosystem-tools.md) | Ecosystem Tools Integration | toolchain | Proposed (revised 2026-06-24) | **D13 (2026-06-24, FLEXIBLE, rip-cage-wlwc.6 + rip-cage-m8zc): any TOOL may declare itself `required` (optional `assert_loaded` check, or codegen-synthesized `test -x <path>` presence); codegen bakes a root-owned `/etc/rip-cage/safety-stack-asserted` declaration of `<id> <base64(check)>` lines; the in-cage safety-stack test runs each baked check generically (no name→check map) and fails loud when a required tool's check fails — closes the silent-guard-loss gap for default cages while `rc` stays tool-agnostic (required-set is manifest DATA, never hardcoded in `rc`). Drop-detection decoupled from the guards' separate behavioral checks; generalized in place from the guard-specific `floor_assert: <guard-id>` field (a new required tool needs zero `rc`/lib edits).** **D9 (revised 2026-06-17, wlwc): forbidden-touch set rewritten in place to the CONTAINMENT floor (sudoers/non-root, egress destination-control+IOC+DNS, fs sandbox, secret-path denylist, git-hooks RO weld, ssh known_hosts mount floor) — "DCG wiring", "PreToolUse hooks", "ssh-bypass blocker" REMOVED; command-string guards are now composable default-on recipes the manifest may compose, bounded by D11's fail-closed validator and protected by D7's host-only property.** Build-time (not runtime) tool composition — the originally-specified build-arg layer (INCLUDE_ toggles, versions.env, --with flags) was never built and is superseded by the host-only manifest; bundled-tool versions are hardcoded Dockerfile ARG defaults; D7–D10 composable host-only tool manifest (4 archetypes incl. MULTIPLEXER, in-cage-only, floor-untouchable, fail-warn daemons); D11 agent-first composability (generic from-source builder stage + fail-closed validator + host-side authoring skill); D2 bundles only what's integral to operating the cage (cm demoted to manifest worked example); **D12 (2026-06-15, FIRM) rip-cage is a composable seam, not a bundler — rc never names/bundles/blesses an optional tool, defaults ship minimal, examples live outside the binary; D9 clarified (multiplexer provider hooks are availability-payload, realized-mechanism note added) + D11 validator bounds them** |
| [006](ADR-006-multi-agent-architecture.md) | Multi-Agent Architecture | multi-agent | Proposed (revised 2026-06-15) | Progressive tiers; D7 re-decided (rip-cage-1f59): the Tier-1a mechanical lever leaves rip-cage for the composed in-cage multiplexer (`session.multiplexer: none` + any manifest-declared provider, default none — manifest-derived not a fixed enum, reconciled 2026-06-15 to ADR-005 D12) — `rc agent`/`rc sessions` removed; rip-cage owns box-entry (`rc up`/`attach`/`exec`), the multiplexer owns spawn/list/supervise; orchestration still lives in the consumer; concurrency agent-specific (pi safe, claude via rip-cage-p1p). D8 (2026-06-14): cage completes the multiplexer's supervise surface by installing each bundled agent's herdr integration via herdr's PUBLIC `integration install` CLI — boundary principle: cage wires tool↔tool only through public CLIs, never hand-placing internals |
| [007](ADR-007-beads-dolt-container-resilience.md) | Beads/Dolt Container Resilience | beads | Accepted | bd wrapper for dynamic port refresh + server-start guard; worktree auto-redirect |
| [008](ADR-008-open-source-publication.md) | Open-Source Publication | release/oss | Proposed | MIT license, scrub personal data, semver, CI lint+build, bash 3.2 compat; D4 local==CI |
| [009](ADR-009-ux-overhaul.md) | UX Overhaul | cli-ux | Proposed | Harm-reduction positioning, auto-build on first up, focused README |
| [010](ADR-010-auth-refresh.md) | Credential Hot-Swap (`rc auth refresh`) | auth | Proposed | Re-extract OAuth from Keychain; in-place credential write preserves inode |
| [011](ADR-011-shell-completions.md) | Shell Completions for `rc` | cli-ux | Proposed | `rc completions` + `rc setup`; default-deny consent for shell integration |
| [012](ADR-012-egress-firewall.md) | Network Egress Firewall | egress/network | Accepted (evolved per 026 — implemented in rip-cage-ta1o) | Pure SNI destination router + default-deny whitelist; observe-mode-first; DNS exfil heuristic + `network.dns.forward_to` seam; D10 legacy-iptables backend is safety-critical on trixie. **D2/D4/D5/D6/D9 evolved per ADR-026** — egress is a pure destination router (no TLS-MITM), L7 content layer delegated to a composed mediator |
| [013](ADR-013-test-coverage.md) | Test Coverage Tiers | testing | Accepted (revised 2026-05-29) | In-container / e2e / host-only tiers; CI = lint+build+host-only; D6 host-only determinism |
| [014](ADR-014-push-less-cage.md) | The Cage Is Push-Less | ssh | Partially superseded | D1/D3 reversed by ADR-017; D2 non-interactive SSH posture + D4 LFS detection remain |
| [015](ADR-015-mise-toolchain-provisioning.md) | Project Toolchain Provisioning | toolchain | Accepted | Mise as invisible plumbing; shared cache volume; auto-install at init |
| [016](ADR-016-cage-host-network-awareness.md) | Cage Host-Network Awareness | config | Accepted | Append cage topology to CLAUDE.md; probe host-bridge hostname; no docker.sock |
| [017](ADR-017-ssh-agent-forwarding-default.md) | SSH-Agent Forwarding On By Default | ssh | Accepted (amended) | Forward by default, scoped by network whitelist; opt-out flag; loud failure on empty agent |
| [018](ADR-018-macos-ssh-agent-discovery.md) | macOS ssh-agent Discovery | ssh | Amended | Probe host candidates; mount the one with keys; launchd convention fallback |
| [019](ADR-019-pi-coding-agent-support.md) | pi-coding-agent Support | pi-agent | Proposed | Container-local pi dir + narrow durable sub-mounts (D1 rev 2026-06-01), no auth-warn, cage topology reference; D4 phase-1 promoted to required; D8 phase-1 SHIPPED (rip-cage-bl1; dcg-gate.ts now loaded via `--no-extensions -e` from its own root-owned path `/etc/rip-cage/pi/`, sn1h + wlwc.4 — was auto-loaded pre-sn1h); D9 bash-only agents reach in-cage daemons via the daemon CLI over bash, not MCP (2026-06-09) |
| [020](ADR-020-ssh-identity-routing.md) | SSH Identity Routing | ssh | Reviewed | Translate host SSH config; four-layer github.com identity resolution; filtered pubkey mount |
| [021](ADR-021-layered-rip-cage-config.md) | Layered rip-cage Config | config | Proposed (revised 2026-06-15) | Two-file substrate: global + project `.rip-cage.yaml`; per-field merge rules; versioning; D6 `session.multiplexer` (default none; allowed-set is `none` + any manifest-declared multiplexer provider, derived from the baked registry — **not** a fixed `tmux`/`herdr` enum, reconciled 2026-06-15 to ADR-005 D12 composable seam) |
| [022](ADR-022-ssh-allowlist.md) | SSH Host + Key Allowlist | ssh | Accepted (evolved) | ssh-agent-filter (agent half) + bash/openssl (host half); `rc reload` for content changes |
| [023](ADR-023-secret-path-mount-denylist.md) | Secret-Path Mount Denylist | safety | Proposed | Default-on pattern denylist for `--env-file` / `.beads` redirect; realpath-first validation |
| [024](ADR-024-prompt-injection-threat-model.md) | Prompt-Injection Threat Model | threat-model | Accepted | Names the threat class: a non-adversarial agent following hostile instructions in content |
| [025](ADR-025-host-adoptable-dcg-policy.md) | Host-Adoptable DCG Policy Layer | safety | Proposed (D2 revised 2026-06-17) | Additive-only DCG policy via `.rip-cage.yaml`; CWD-anchor wrapper + pinned DCG_CONFIG close the `/workspace/.dcg.toml` self-disable hole. **D2 (revised 2026-06-17, wlwc): DCG is NO LONGER part of the welded uncrossable floor — that floor is now CONTAINMENT (ADR-026 D1/D2). DCG becomes a composable, default-on safety recipe (binary via manifest + guard wiring at an agent-unwritable load path + recipe); uncrossable-from-inside preserved in the `ro`-default config via unwritable load path + host-side composition. The DCG config floor (additive-only, can't downgrade core) and the D3 wrapper/engine are RETAINED** |
| [026](ADR-026-containment-mediation-identity.md) | Containment + Delegated Mediation Seam | containers / egress | Accepted (D2 + D5 revised 2026-06-17) | **D2 (revised 2026-06-17, wlwc): the ssh-bypass command HOOK reclassified OUT of containment to a composable default-on command-guard recipe (sibling to DCG, ADR-025 D2); welded ssh containment = the known_hosts/config mount floor (ADR-022 D4) — full ssh destination scoping = welded mount floor + default-on ssh-bypass recipe; DNS exfil guard stays containment via the tie-break.** rip-cage = containment (controls WHERE traffic goes + guarantees the chokepoint), delegates mediation (WHAT is said — L7 policy, credential injection) to a composed external mediator; layers sorted by maintenance drift-rate; standalone = accident-containment, exfil-grade needs composition. **D5 (FIRM): mediation is a composable-provider seam** — `network.egress.mediator` (default none, manifest-derived allowed-set) + a MEDIATOR archetype + the `network.http.forward_to` HTTP-CONNECT handoff; *selection/forward* isomorphic to the session-multiplexer pattern but the *launch* leg is host-driven root (sibling to the firewall, not the multiplexer); push-side so the floor is uncrossable; bundles nothing (mitmproxy = validation target, iron-proxy = recommended adopt; clawpatrol reclassified to alternative appliance — WG-only ingress can't plug the seam). D5 lifted EXPLORATORY→FIRM on the in-cage E4 proof + real-`rc up` auto-launch (rip-cage-ta1o.5) |
| [027](ADR-027-agent-substrate-projection.md) | Agent-Substrate Projection into the Cage | pi-agent / composition | Proposed (D1 FIRM, D3 FIRM 2026-06-17; D3 mechanism reversed 2026-06-23) | What host agent-substrate is admissible into a cage and how it's projected. **D1 (FIRM; evolved 2026-06-17, wlwc): admission rests on containment + host-ownership, not on excluding executable code — invariant (a) is now per-ASSET `ro`/`rw`, `ro` DEFAULT, `rw` opt-in live-write-through for self-improvement; uncrossable-from-inside holds in the `ro`-default config, `rw` is a knowing relaxation backstopped by containment; projected-extension validator retained for `rw`-projected extensions.** config/credentials/runtime-state stay cage-owned. D2 (FLEXIBLE — being RETIRED 2026-06-17 by the now-FIRM D3 seam): pi instruction-content + the `subagent` extension projected as a hardcoded per-bundled-agent block, symmetric with CC — interim debt on a now-active retirement path. **D3 (FIRM — lifted 2026-06-17; mechanism reversed 2026-06-23, wlwc): agents, substrate, AND guards compose from the generic `TOOL` + per-asset `ro`/`rw` mount seams — NO agent archetype, no `agents.enabled`, no per-agent floor-lock slot; the floor-lock is the `ro`-mount of guard wiring on a separate path (mechanism owned by D1) + a per-recipe launch constraint (CC airtight, pi direct-binary an accepted residual to keep olen retired); dcg + ssh-bypass demote to composable from-source recipes; the published image composes CC+pi+dcg via a default config (blessing at distribution layer, not seam/code; agent-performed composition, no `compose:` directive per ADR-005 D12); the fail-closed floor-protection validator is retained on the mount seam; warrant (unchanged) = rip-cage is published for heterogeneous harnesses (codex/crush/…), so the third agent is the users' harnesses.** Siblings: rip-cage-olen (**retired** — re-done as the separate-path floor-lock, rip-cage-wlwc.4: pi guard root-owned at `/etc/rip-cage/pi/`, `extensions/` agent-owned), rip-cage-r9n4 (CC instance, **closed 2026-06-22** — CC hook-registration vector closed via managed-settings) |
