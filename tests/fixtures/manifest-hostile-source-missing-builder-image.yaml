version: 1
tools:
  # Hostile fixture: from-source TOOL missing build_source.builder_image.
  # _manifest_validate must reject this naming 'builder_image'.
  - name: missing-builder-image-tool
    archetype: TOOL
    version_pin: "1.0.0"
    egress: []
    mounts: []
    build_source:
      build_script: "tests/fixtures/build-hello-from-source.sh"
      output_path: "/usr/local/bin/hello-from-source"
