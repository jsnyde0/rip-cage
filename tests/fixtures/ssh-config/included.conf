# File included from host-config-full.conf via Include directive.
# Its IdentityFile paths should also be rewritten.
Host internal
  HostName internal.example.com
  User jonat
  IdentityFile ~/.ssh/id_ed25519_internal
  Match exec "some-host-binary --check"
