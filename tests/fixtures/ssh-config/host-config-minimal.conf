# Minimal fixture with no macOS directives, no host-only directives.
# Used to verify idempotency and basic path rewriting only.
Host my-server
  HostName myserver.example.com
  User admin
  IdentityFile ~/.ssh/id_ed25519_personal
